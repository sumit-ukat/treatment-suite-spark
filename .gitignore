node_modules
dist
.output
coverage
*.local

# Env - never commit real values
.env
.env.*
# ...but the template must be committed, or nobody can configure the project.
# It holds only the development URL and publishable key, both public by design.
!.env.example

# Supabase CLI local state
supabase/.branches
supabase/.temp

# --- Never commit real client data ---
# The source whiteboard contains real client names and embedded photographs.
*.xlsx
*.xlsm
*.xls
Whiteboard*
/private-data/
/import-samples/
**/client-photos/
